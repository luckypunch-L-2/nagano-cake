<div class="genre_search">
  <teble class="genre_table">
    <thead>
    <tr>
      <th>ジャンル検索</th>
    </tr>
    </thead>
    <tbody>

      <% genres.each do |genre| %>
        <tr>
          <td>
          <%= link_to genre.name, items_path(genre_id: genre), method: :get %>
          </td>
        </tr>
      <% end %>
      </tbody>
  </teble>
</div>